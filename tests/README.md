# PBOS v0.1 Validation Runner

Run from repository root:

    python3 scripts/validate_pbos_v0_1.py

The runner checks required contracts, canonical identity, domain boundaries, human authority, Codex governance markers, provenance presence, and JSON schema syntax.

The validator is deterministic and read-only with respect to canonical architecture.
